namespace ModelPainter.Model.Java;

public class CompiledJavaModel
{
	public static CompiledJavaModel Load(string filename)
	{
		// using var fs = new FileStream(filename, FileMode.Open, FileAccess.Read, FileShare.Read);
		// var classFile = JavaClassFile.Read(fs);
		return null;
	}
}